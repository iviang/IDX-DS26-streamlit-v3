# IDX-DS26-streamlit-v3
Version 3 of the streamlit app which instead of relying on one model, switches between the top two models (LGBM-log, Ensemble stack-ridge). LGBM-log covers the luxury tail while Ensemble is better for mid-market typical home prediction.
